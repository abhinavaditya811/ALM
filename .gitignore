__pycache__/
*.pyc
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/
build/
dist/
.venv/
.env

# Runtime harness state (version registry, correction candidate pool):
# operational data generated by actually running the harness, not source
# code or the frozen eval set. Not committed until a real (non-placeholder)
# scoring run produces something worth keeping as the audit trail.
data/registry/
data/corrections/
